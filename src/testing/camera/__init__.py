"""Camera / vision isolation test suite.

Each module here tests ONE camera responsibility on its own so a failure
points at exactly one subsystem. See testing/camera/README.md for the full
run order, expected output, known bugs + fixes, and the exhaustive
sign-off checklist.
"""
