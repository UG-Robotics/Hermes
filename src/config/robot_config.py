# Normalized default speed and steering values (-1.0 to 1.0).
DEFAULT_SPEED = 0.35

# Steering offset applied for obstacle avoidance.
DEFAULT_STEERING_OFFSET = 0.35

# Speed reduction when avoiding obstacles.
OBSTACLE_SPEED_SCALE = 0.6
